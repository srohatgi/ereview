/*
 * JS for BusinessReview generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return '1e33c169-3f5e-4ddc-840e-e1b741a045b8';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Appery.adjustContentHeight();
}

function adjustContentHeightWithPadding() {
    Appery.adjustContentHeightWithPadding();
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

Appery.AppPages = [{
    "name": "startScreen",
    "location": "startScreen.html"
}, {
    "name": "BusinessReview",
    "location": "BusinessReview.html"
}, {
    "name": "ServiceReview",
    "location": "ServiceReview.html"
}];

BusinessReview_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'mobilelabel_1': 'BusinessReview_mobilelabel_1',
        'mobilesearchbar_products': 'BusinessReview_mobilesearchbar_products',
        'spacer_34': 'BusinessReview_spacer_34',
        'mobilelist_35': 'BusinessReview_mobilelist_35',
        'mobilelistitem_36': 'BusinessReview_mobilelistitem_36',
        'mobilelistitembutton_37': 'BusinessReview_mobilelistitembutton_37',
        'mobilelist_42': 'BusinessReview_mobilelist_42',
        'mobilelistitem_43': 'BusinessReview_mobilelistitem_43',
        'mobilelistitembutton_44': 'BusinessReview_mobilelistitembutton_44',
        'mobilelabel_49_ProductCode': 'BusinessReview_mobilelabel_49_ProductCode',
        'mobilebutton_50': 'BusinessReview_mobilebutton_50'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=page] div[data-role=footer]").css("bottom", "-36px");
    }

    if (Appery.getTargetPlatform == "I") {
        $.each(window.n2id, function(name, id) {
            var elApperyRole = $("#" + id).attr("data-role");
            if (elApperyRole != "appery_label" && elApperyRole != "appery_link") {
                var appleDataSelector = "#" + id + "[x-apple-data-detectors], #" + id + " [x-apple-data-detectors]";
                $(appleDataSelector).attr("x-apple-data-detectors", "false");
            }
        });
    }

    Appery.CurrentScreen = 'BusinessReview';

    /*
     * Nonvisual components
     */
    var datasources = [];

    SalesforceProduct2_query_service = new Appery.DataSource(Salesforce_Product2_query_service, {
        'onComplete': function(jqXHR, textStatus) {

            $t.refreshScreenFormElements("BusinessReview");
        },
        'onSuccess': function(data) {
            try {
                SalesforceProduct2_query_service.execute({})
            } catch (ex) {
                console.log(ex.name + '  ' + ex.message);
                hideSpinner();
            };
        },
        'onError': function(jqXHR, textStatus, errorThrown) {},
        'responseMapping': [{
            'PATH': ['$'],
            'ID': 'mobilelistitem_36',
            'SET': []
        }, {
            'PATH': ['$', 'records[0]', 'ProductCode'],
            'ID': 'mobilelist_42',
            'ATTR': 'visible'
        }, {
            'PATH': ['$', 'records[0]', 'Product_Ratings'],
            'ID': 'mobilelist_42',
            'ATTR': 'visible'
        }],
        'requestMapping': [{
            'PATH': ['Authorization'],
            'TYPE': 'STRING',
            'HEADER': true,
            'ATTR': '{salesforce_access_token}'
        }, {
            'PATH': ['q'],
            'TYPE': 'STRING',
            'ID': 'mobilesearchbar_products',
            'ATTR': 'value',
            'TRANSFORMATION': function(value) {

                return "Select id,Name,ProductCode,Product_Ratings from product2 where Name like '%" + value + "%'";
            }
        }, {
            'PATH': ['appery-proxy-url'],
            'HEADER': true,
            'ATTR': '{salesforce_instance_url}/services/data/{salesforce_api_version}/query'
        }, {
            'PATH': ['appery-key'],
            'HEADER': true,
            'ATTR': '1384839519730'
        }, {
            'PATH': ['appery-rest'],
            'HEADER': true,
            'ATTR': '2780546'
        }]
    });

    datasources.push(SalesforceProduct2_query_service);

    /*
     * Events and handlers
     */

    // Before Show
    BusinessReview_beforeshow = function() {
        Appery.CurrentScreen = "BusinessReview";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_3C16_onLoad = BusinessReview_onLoad = function() {
        screen_3C16_elementsExtraJS();

        // TODO fire device events only if necessary (with JS logic)
        BusinessReview_deviceEvents();
        BusinessReview_windowEvents();
        screen_3C16_elementsEvents();
    }

    // screen window events
    screen_3C16_windowEvents = BusinessReview_windowEvents = function() {

        $('#BusinessReview').bind('pageshow orientationchange', function() {
            adjustContentHeightWithPadding();
        });

    }

    // device events
    BusinessReview_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

        });
    }

    // screen elements extra js
    screen_3C16_elementsExtraJS = BusinessReview_elementsExtraJS = function() {
        // screen (BusinessReview) extra code

        /* mobilelist_35 */

        listView = $("#BusinessReview_mobilelist_35");
        theme = listView.attr("data-theme");
        if (typeof theme !== 'undefined') {
            var themeClass = "ui-btn-up-" + theme;
            listItem = $("#BusinessReview_mobilelist_35 .ui-li-static");
            $.each(listItem, function(index, value) {
                $(this).addClass(themeClass);
            });
        }

        /* mobilelistitem_36 */

        /* mobilelist_42 */

        listView = $("#BusinessReview_mobilelist_42");
        theme = listView.attr("data-theme");
        if (typeof theme !== 'undefined') {
            var themeClass = "ui-btn-up-" + theme;
            listItem = $("#BusinessReview_mobilelist_42 .ui-li-static");
            $.each(listItem, function(index, value) {
                $(this).addClass(themeClass);
            });
        }

        /* mobilelistitem_43 */

    }

    // screen elements handler
    screen_3C16_elementsEvents = BusinessReview_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

        $('#BusinessReview_mobilecontainer [name="mobilesearchbar_products"]').die().live({
            keypress: function() {
                try {
                    SalesforceProduct2_query_service.execute({})
                } catch (ex) {
                    console.log(ex.name + '  ' + ex.message);
                    hideSpinner();
                };
            },
        });
        $('#BusinessReview_mobilesearchbar_products-cover-img').live({
            vclick: function() {
                if (!$(this).attr('disabled')) {
                    try {
                        SalesforceProduct2_query_service.execute({})
                    } catch (ex) {
                        console.log(ex.name + '  ' + ex.message);
                        hideSpinner();
                    };
                }
            }
        });

        $('#BusinessReview_mobilecontainer [name="mobilebutton_50"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    CustomURLAjax();

                }
            },
        });

    }

    $("#BusinessReview").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        BusinessReview_beforeshow();
    });

    if (runBeforeShow) {
        BusinessReview_beforeshow();
    } else {
        BusinessReview_onLoad();
    }

}

$("#BusinessReview").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    BusinessReview_js();
});